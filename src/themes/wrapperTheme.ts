import { createTheme } from '@mui/material/styles';

const WrapperTheme = createTheme ({
    palette: {
        background: {
            default: '#BB9457' 
        },
    },
});

export default WrapperTheme;